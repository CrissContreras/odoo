<odoo>
<!-- respuesta 1 -->
    <data>
        <template id="report_visit_card">
        <!-- esto se debe revisa -->
        <!-- aqui va desde req -->
            <t t-call="web.html_container">
                <t t-foreach="docs" t-as="o">
                    <t t-call="web.external_layout">
                        <div class="page">
                            <h2>Visita</h2>
                            <p>Cliente:
                                <span t-field="o.customer.name"/>
                            </p>
                            <p>Descripción:
                                <span t-field="o.name"/>
                            </p>
                            <p>Fecha:
                                <span t-field="o.date" t-options='{"format": "dd/MM/yyyy"}'/>
                            </p>
                        </div>
                    </t>
                </t>
            </t>
        </template>
    </data>
</odoo>
<!-- respuesta 2 -->
<!-- esto esta en la rama requerimiento y debe pasar a la rama dev -->
